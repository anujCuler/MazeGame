#include "player.h"

Player::Player(int startX, int startY) {
    x = startX;      // Set player's x position
    y = startY;      // Set player's y position
    score = 0;       // Set starting score
    moves = 0;       // Set starting move count
}

